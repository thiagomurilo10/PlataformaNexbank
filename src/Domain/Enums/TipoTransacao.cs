namespace PlataformaNexbank.Domain.Enums;

// Representa os tipos possíveis de movimentação financeira em uma conta.
public enum TipoTransacao
{
    Deposito, // Entrada de valor na conta
    Saque     // Saída de valor da conta
}